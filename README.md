# 质量分析平台（Quality Analytics Platform）

类 Minitab 的工厂质量部门统计分析工具 — 依据 `../design_handoff_quality_platform/` 交接文档,
从设计原型 `质量分析平台.dc.html` 重新实现为 **React 18 + TypeScript + Vite** 应用。

## 运行

```bash
npm install
npm run dev            # Web 端 http://localhost:5188
npm test               # vitest 统计引擎与状态回归测试
npm run build          # Web 生产构建
npm run tauri dev      # 桌面端开发（需 Rust 工具链）
npm run tauri build    # 桌面端打包（macOS 产 .app/.dmg，Windows 产 .msi/.exe）
```

## Windows 桌面端打包

桌面壳为 **Tauri 2**（`src-tauri/`），Web 与桌面共用同一份 UI 与统计引擎。
Windows 安装包需在 Windows 上构建（或走 CI 交叉产出）：

- **CI（推荐）**：仓库已带 `.github/workflows/build-windows.yml`，推 `v*` tag 或手动
  触发 workflow_dispatch，在 windows-latest 上产出 `.msi`（WiX，中文）与 `.exe`
  （NSIS，简中、perMachine），从 Actions Artifacts 下载。
- **Windows 本机**：装 Rust（MSVC 工具链）+ Node 后 `npm ci && npx tauri build`。
- macOS 本机 `npm run tauri build --bundles app,dmg` 可出 mac 版用于开发验证。

平台差异全部收敛在 `src/platform/adapter.ts`（PlatformAdapter，交接文档 §4.2）：

| 能力 | Web | 桌面 (Tauri) |
|---|---|---|
| 导出报表 | Blob 下载 | 原生保存对话框 + Rust `save_text_file` 落盘 |
| 导入数据 | `<input type=file>` | 原生打开对话框 + Rust `read_text_file` |
| 导出载荷 | Excel .xlsx；图文 HTML（打印为 PDF）；Word .docx；PowerPoint .pptx | 同左 |

## 结构

```
src/
  core/          纯 TS 统计引擎（零 UI 依赖，可单元测试，两端共享）
    basicMath.ts   erf / phi / invNorm (Acklam) / binomCdf / mean / stdev / median / quantile
    spc.ts         控制图常数 (n=2..10) + Nelson 判异准则引擎 (准则 1–8)
    capability.ts  Cp/Cpk/Pp/Ppk/CPU/CPL/Z.bench/西格玛水平/PPM
    doe.ts         2³ 全因子效应 + Lenth 检验 + 主效应/交互均值
    aql.ts         GB/T 2828.1 单次抽样（字码/n/Ac/Re、OC 曲线、RQL、α/β）
    calculator.ts  计算器按键逻辑
    sampleData.ts  确定性演示数据（LCG 种子 20260601 等，与原型逐位一致）
    __tests__/     对拍测试（关键校验值来自交接文档）
  store/appStore.ts  zustand 全局 UI 状态（交接文档 §6）
  platform/          PlatformAdapter 双端适配（导入/导出）+ 报表生成
src-tauri/           Tauri 2 桌面壳（Rust：文件读写命令 + dialog 插件）
  ui/
    tokens.ts      图表三主题（经典/现代/高对比，§7）
    charts/        手写 SVG 图表（控制图/直方图/帕累托/箱线/DOE 四图/OC 曲线）
    shell/         菜单栏/工具栏/左导航/状态栏/弹窗/Toast（§5.0/5.10/5.11）
    pages/         九个模块页面（§5.1–5.9）
```

## 关键校验值（npm test 全部覆盖）

| 场景 | 期望 | 状态 |
|---|---|---|
| 过程能力 Cpk（规格 24.90/25.10） | ≈ 1.36 | ✅ |
| AQL(国标主表)：N=120, 水平 II, AQL=1.0 | 初始字码 F，↑箭头改档 → E, n=13, Ac=0, Re=1 | ✅ |
| DOE Lenth | A 温度、C 时间显著；交互项不显著 | ✅ |
| 计算器 | 144 ÷ 12 = 12 | ✅ |
| SPC 判异 | 子组 16–18、22 失控（准则 1） | ✅ |
| 单因子 ANOVA（实算） | P ≈ 0.012 显著,设备 B 均值最高 | ✅ |
| Gage R&R（交叉 ANOVA 法实算） | 合计 GRR < 10% 可接受 | ✅ |
| F 分布 CDF | F(2,6)=3 → p=0.125（教科书值） | ✅ |
| 控制图常数 | n=2..10 全表 | ✅ |

## 数据导入（真实闭环）

「导入 CSV/Excel」或剪贴板粘贴即可替换活动数据集：自动识别分隔符/表头/数值列，并保留文本分组列。
SPC 可选择行式子组、列式子组、单值列，或「测量值+子组 ID」长表；自动模式仅在角色唯一时推断，
单值自动使用 I-MR。SPC/能力/仪表盘/工作表/导出报告按同一数据角色实时重算。

## 图表主题

三套主题（经典/现代/高对比）+ 网格开关：菜单「工具」内切换,或点击状态栏
「图表风格」标签循环切换。字体已本地打包（@fontsource),桌面端离线可用。

## 功能全景（v1.34）

- **数据**：CSV/剪贴板/**真 .xlsx 工作簿**导入（变量矩阵 n=2..10 / P 图不良数 /
  C 图缺陷数），文本列保留为分组列；**手工录入**（双击单元格编辑、添加子组、悬停
  行号删行,编辑演示集自动转「副本」）；localStorage 持久化 + 最近数据集 +
  **规格限随数据集记忆**；MES 模拟采集流（实时追加子组,SPC 滚动更新）。
- **SPC**：X̄-R / X̄-S / I-MR / **EWMA**（时变限）/ **CUSUM**（双侧累积和）/ P / C,
  Nelson 准则 1–8（并按属性图/变异图适用性限制），展示规则定义、来源、点选明细和分阶段控制限。
- **能力**：Cp/Cpk/Pp/Ppk/CPU/CPL/**Cpm**/Z.bench/σ 水平/PPM;**Anderson-Darling
  正态性检验 + 正态概率图**;非正态时 **Box-Cox** λ 寻优与变换后能力。
- **假设检验**：单因子 ANOVA + **单样本/双样本 t 检验**（Welch,含 95% CI）+
  **相关与回归**（散点图 + 最小二乘 + r/R²/斜率显著性）;引擎实算（F/t 分布 CDF
  经不完全贝塔）;导入数据按所选响应/因子角色实算，形态不满足时明确拦截，不再静默回落无关演示研究。
- **帕累托 / 鱼骨图**：帕累托图 + **6M 因果图**（三上三下鱼骨,问题与原因可编辑,
  localStorage 持久化并随历史记录精确恢复）；内置鱼骨始终标为示例，不冒充用户根因结论。
- **AQL**：表 1 + 正式表 **2-A/2-B/2-C** 方案实时重算、OC 曲线、实际不合格数批判定、责任追溯及正式转移得分/暂停规则。
- **导出（四格式共用当前分析）**：Excel **.xlsx**、图文 HTML（打印为 PDF）、Word **.docx**、PowerPoint **.pptx**
  共用当前 AQL / ANOVA / 单双样本 t / 回归 / DOE / SPC / 能力 / Gage R&R / 帕累托 / 鱼骨图的
  结论、参数、原始分析口径表格和图形，不混入无关演示或通用占位结论；
  「图形 → 下载本页图表 PNG」批量导出当前页图表;Ctrl/Cmd+S/O/P/N 快捷键。
- **工程**：TypeScript + ESLint + Vitest 全量回归；`ci.yml` 每次 push 质量门；
  `release.yml` 推 v* tag 自动发 GitHub Release(含更新器签名 latest.json);
  桌面端「帮助 → 检查更新」经 tauri-plugin-updater 对接 Releases。

## MSA 工厂案例验收（v1.40.0,批次MSA-716,未提交/未发布工作树）

依据电机一厂验收包（MSA案例数据源7.18.xlsx + MSA案例结果5个7.18.pptx,5 案例=3 单人+2 三人,3 OK/2 NG）与《需求理解与开发计划_Claude_MSA工厂案例验收.md》（经 Codex 交叉审核修订）实现,5 案例数值与 Minitab 17 输出**逐位一致**：

- **单操作员研究（完成）**：操作员列可为常数或选「无（单操作员）」,退化为单因子随机效应模型,GRR=重复性、不估计再现性;图形报告单人 4 联（变异分量/R图/按部件/Xbar）、多人 6 联（另加按操作员箱线与部件×操作员交互,R/Xbar 按操作员分组）。
- **会话窗口数值表（完成）**：来源/标准差(SD)/研究变异(6×SD)/%贡献/%研究变异/%公差(SV/Toler) 六列,再现性下嵌套操作员/交互分行,过程公差行、可区分的类别数 ndc=⌊√(2σ²部件/σ²GRR)⌋（下限 1）与「〈测量项〉 的量具 R&R」标题,与 Minitab 会话窗口同构。
- **过程公差四模式（完成）**：联动规格限（双开=宽度,单开=单侧）/双侧宽度/仅上限/仅下限/不设;单侧按 Minitab 单边口径 %公差=3×SD/|限值−总均值|,总均值已达/越过限值或距离为浮点噪声时明确拒绝并说明（阈值为纯相对量,不误伤大/小量纲数据）。
- **判定双标准（完成）**：工厂标准（主要 %研究变异 与 次要 %公差 均 ≤10% 理想/≤20% 可接受,取较差;默认）与 AIAG（<10/10–30/>30）可切换,主结论只突出所选口径、另一口径作对照;计算与判定分离,标准随快照/.qproj 持久化。
- **多块工作簿导入（完成）**：detectCsvBlocks 识别「说明区+多案例并排」工作表,文件/剪贴板两路先选块再导入,不整表瞎猜;常规单表不受影响。
- **全链路一致（完成）**：computeGageRR 公差参数升级为 {mode,value},5 个调用点（App 状态栏/MSA 页/report/专项报告/保存摘要）全部同源;gageTolMode/gageTolValue/gageStandard 进 appStore persist（写入与水合 merge 对称）+.qproj 白名单/枚举校验+分析快照,v1.40 以前的旧快照按保存时行为回放（双侧宽度或不设+AIAG）。
- **验证状态**：黄金夹具三层精度（原始数据 TSV+独立 Python 复算高精度期望（相对 1e-9）+PPT 截图舍入层逐位全等）,含真实工厂 xlsx 端到端导入链路测试;三镜头对抗审查（引擎数学/调用点一致性/UI 导入）确认问题当场修复（单侧守卫量纲阈值、assessGage 公差缺席归因、persist merge 漏三字段、面板极差展开栈溢出等）;819 项 Vitest+13 项 Rust 全绿,TS/ESLint/构建通过,浏览器逐案例复验（案例1/2/3/4 实测,含单侧 17.23 与工厂/AIAG 双口径分歧案例4）。此工作树未 commit、未 tag、未发布,供独立审查差异。
- **待工厂确认**：案例5 报告标题若须与 PPT 全等（「反电势值4」）,导入后改列名即可,零开发。

## Codex 独立复验整改（v1.39.0,未提交/未发布工作树）

依据人工审核716、v1.37 审计报告、v1.38 独立复验报告逐项整改；保留 v1.38 已通过项，重点修复六条未闭环/新发现问题：

- **AQL 责任账本（完成）**：S-1～S-4 特殊检验水平纳入记录规范化、localStorage 和 qproj 全链，避免合法记录在保存/重放时被静默丢弃。
- **AQL 质量表示（完成）**：质量表示改为显式业务字段；百分不合格品允许 AQL≤10，每百单位不合格数允许完整 26 档（包括 AQL≤10）。页面、状态机、责任记录、历史摘要、项目文件、OC/RQL 与专项报告全部同源；旧项目按 v1.38 行为迁移。
- **per100 导出（完成）**：泊松 OC 曲线提为页面/专项报告共享组件，导出不再只给数据而缺图。
- **DOE 分数因子（完成）**：新增 2^p 分部选择和负生成元留痕；折叠前检查镜像是否增加设计点，拦截 2^(4−1)、2^(6−1)、2^(6−2)、2^(7−3) 的无效全折叠；运行数与分辨率均按实际合并矩阵计算。
- **DOE 创建计划（完成）**：新增“将设计存储在工作表中”开关；关闭时只生成可核对预览，不覆盖当前数据集或跳转页面。
- **MSA 工作表格式（完成）**：混合数值/文本列按 Minitab 格式显示 `C1 / C2-T / C3`；公式计算与条件筛选同步使用相同 C# 映射，避免只改表头产生错列计算。
- **验证状态**：新增 AQL/DOE/MSA/导出防回归测试；72 个 Vitest 文件、788 项测试及 13 项 Rust 测试全绿，TypeScript/Vite/PWA 生产构建与 ESLint 通过，并完成 AQL/DOE 本地真实界面复验。此工作树未 commit、未 tag、未发布，供 Claude 独立审查差异。

## 人工审核716 + Codex v1.37 审计遗留全量整改（v1.38.0,批次716-R3）

按 Codex v1.37 审计遗留清单与人工审核716 未闭环项整改。**逐项状态如实标注(完成/部分完成/待外部条件),
不使用「全部闭环」表述**:

- **AQL 26 档(完成)**:新增 15–1000 十档「每百单位不合格数」体系——泊松 OC(poissonCdf 双算法互证)、
  d 可大于 n、体系分流(≤10 二项 / ≥15 泊松)、RQL 以 λ 计;表 2-A/2-B/2-C 十列按国标扫描件
  **双盲转录(490 格零分歧)+ 工厂 xlsx 三方机器 diff + 仲裁像素复核**入黄金夹具
  (勘误#3:2-B S 行工厂 xlsx 误填 ↑,国标留白);16 档区数据与行为零变化。
- **DOE 多水平+分数因子(完成)**:一般全因子(每因子 2–5 水平,Type III Adj SS 对齐 Minitab GLM)、
  分数因子 2^(k−p)(k=3–7,标准生成元/分辨率表/设计选择表)、折叠(不折叠/全折叠/指定因子),
  折叠块作区组;分析页/保存卡/专项报告三处同源(自查修复 4 因子截断口径分裂)。
- **X̄-R 合并双面板(完成)**:X̄ 与 R(及 X̄-S)合并为上下双面板单标题、共享横轴;
  I-MR 因 MR 少一点保持独立卡(注明原因)。
- **MSA 两项(完成)**:删除重复的 ID/行 数据列(只保留 Minitab 式行号槽);
  角色推荐改为可解释打分(列名关键词/类别基数/交叉平衡),仅预填、用户确认,
  页面/状态栏/保存记录/专项报告经 effectiveGageSelection 同源;
  旧记录回放固化保存当时的位置默认口径,不受推荐算法影响。
- **能力口径全局统一(完成)**:副标题/底部状态栏/首页 Cpk·PPM 卡全部改消费能力口径解析器,
  与能力页/保存记录/报告同源;首页卡在非默认口径时标注「能力口径:单列×子组」。
- **P 图自定义 K1 双侧截断(完成)**:evalLimitedRules 新增逐点真 σ 参数,SPC 页/保存卡/专项报告
  三处 P/C 判异全部直传 √(p̄(1−p̄)/nᵢ),不再从钳位限值反推。
- **对抗自查(四镜头+逐条证伪)**:报 10 条、确认 10 条(去重 6 个修复点)全部修复,
  核心两条为 5–7 因子分数因子的保存卡/专项报告 4 因子截断口径分裂;回归测试 +12,共 778 全绿。
- **仍待外部条件(如实声明)**:MSA GRR 数值与 Minitab 一致性验收需工厂原始工作簿+Minitab 输出;
  per100 体系的导出报告暂以数据表述为准(页面有专用泊松 OC 曲线,导出版另批提为共享组件);
  AQL 全表第二份完全独立人工转录(Grok)可作为后续加固项。

## Codex v1.36 验收缺陷整改（v1.37.0,批次716-R2）

Codex 对 v1.36.0 的逐项验收报告全部指控经对抗验证成立,本批全数修复:

- **P0 · 能力规格绑定能力口径**:规格记忆键改为跟随能力口径解析出的测量特性
  (默认「沿用 SPC 角色」时与原 SPC 键一致);切换能力测量列/子组模式时自动恢复该特性的
  记忆规格或按 μ±4σ 重建议并 toast 提示;新增 1000σ 量纲哨兵
  (capabilityInputError,页面/记录/仪表盘/报告四路共用)——切列后沿用旧量纲规格不再产出
  Cpk=19928 之类的虚假「能力充足」结论;能力页编辑规格不再反向污染 SPC 列的记忆键。
- **P2 · P 图自定义 K1 截断误报**:evalLimitedRules 的 σ̂ 改取两侧限距最大值
  (单侧钳位时另一侧恒为真 3σ 距),P 图 UCL 截断到 1 与 MR/R/S/C 图 LCL 钳位 0 两类场景
  均不再误报(Codex 探针 p̄=0.9,n=10,p=1 已入回归测试)。
- **P2 · 判异说明动态文案**:说明区改用 ruleKDesc 随当前 K 输出
  (如 K2=7 →「连续 7 点落在中心线同一侧 [自定义 K,标准值 9]」),不再出现 9 与 7 并存。
- **P1 · MSA 工作表列序**:TextColumn 新增 sourceIndex(导入原列位),解析/存储/qproj/
  分析快照全链路贯通,工作表按导入原序交错显示(部件、测试人、螺钉高度);
  首列在非行式口径下显示「行」(对齐 Minitab 行号槽),行操作文案同步。
- **对抗自查追加修复 6 处**(三镜头审查 9 报 8 实):Box-Cox 变换域先过输入闸门
  (负 λ 把近零规格映射成巨值时跳过补充卡,不再炸整页)、四个 legacy 导出 builder
  (HTML/xlsx/docx/pptx/文本)哨兵状态下能力章节写「未运行」其余章节照常导出、
  删除数值列后文本列 sourceIndex 按删除前显示序重编号、历史回放保留 sourceIndex、
  口径零变化的点击不触发规格同步(保护回放快照规格)+ legacy 数据集名记忆兼容、
  行操作文案随口径。
- 回归测试 +16(共 708 全绿);tsc/eslint/build 干净;四项主修复均经浏览器实测。

## 第三轮人工反馈(716)第一轮落实（v1.36.0）

按《产品修改计划_第三轮人工反馈716》v2 执行批次 A′/K1/L/M/N/P 与 O 部分:

- **K1 · AQL 十六档全表**:AQL 覆盖 GB/T 2828.1 正式主表全部 16 档(0.010–10),
  检验水平新增特殊水平 S-1~S-4;表 2-A/2-B/2-C 全 26 档转录为 16 档黄金夹具
  (PDF SHA-256 锁定,含 2-C Q/0.025 勘误:工厂 xlsx 的 ↓ 应为长 ↑ → N/200/0/1);
  UI 提供 6 个常用 AQL 快捷芯片 + 全 16 档下拉。
- **M · 判异准则 K 值可编辑**:对齐 Minitab「Xbar-R 选项→检验」,8 条准则的 K 值均可编辑
  (默认 Nelson/Minitab 标准值,非标准值显著标注「自定义判异参数」);K 值进快照/qproj/报告,
  页面-工作表高亮-仪表盘-专项报告-能力评估共用同一 K。
- **N · 能力分析子组显式配置**:能力页新增「沿用 SPC 角色 / 单列+子组大小(常量) /
  单列+子组 ID 列」三模式,组内 σ 随子组定义变化并明示口径(末组不足披露)。
- **A′ · ANOVA 遗留两项**:残差诊断四合一改为可选单独图表(正态/拟合值/直方图/顺序);
  个体值图与区间图增加组均值连接折线(对齐 Minitab)。
- **P · 经典主题 Minitab 化**:白底 1px 边框、去网格、系列深藏青 #2A4B8D、UCL/LCL 红实线、
  中心线黑、右侧黑字标签含等号、1-2-5 圆整刻度;帕累托统一钢蓝零间距柱+描边、深蓝累积线、
  左轴量程=累计总数、底部 类别/频数/百分比/累积% 统计表、标题「〈列名〉 的 Pareto 图」。
- **L · MSA 修复**:GRR 报错指名道姓(缺哪个操作员/部件/胞);整数 ID 列显示 0 位小数。
- **O · DOE 增强(部分)**:因子数改数字输入、随机化种子、仿行数;效应图三选
  帕累托/正态/半正态(Lenth PSE 参考线);未编码单位回归方程(含 Ct Pt);
  残差类型三选 正规/标准化/删后 + 四合一/单独布局 + 残差 vs 变量。
- **对抗审查整改**:三路独立审查(核心统计/状态持久化/UI 主题)确认 14 项缺陷全部修复
  (含 evalLimitedRules k1≠3 在钳位 LCL 图上的 σ̂ 误报、能力子组口径进快照、
  stacked 子组 ID 角色引用、经典主题语义参考线消失等),新增防回归测试 7 条,692 测试全绿。
- 暂缓项(计划内后续批次):K2 每百单位不合格数体系(AQL 15–1000)、DOE 折叠/分部/
  多水平因子/分辨率表、X̄-R 合并双面板。

## Codex v1.34 修改意见落实 · PWA 功能测试整改（v1.35.0）

按 Codex《修改意见_Codex_v1.34.0_给Fable》中与 PWA 功能测试相关的 7 项完成整改
（平台签名与桌面端专属项按用户决定暂缓；P0-2 账本重放验证列为独立批次）：

- **能力结论唯一判定器（P0-4）**：新增 `core/capabilityAssessment.ts`，能力页报告卡、
  保存分析记录、仪表盘 Cpk 卡、项目汇总徽章与专项报告全部消费同一评估结果——
  过程失控时任何界面都不再显示绿色「能力充足」，统一为「过程不稳定 · 能力值仅描述样本」。
- **持久化失败不再被成功提示掩盖（P0-5）**：数据导入返回结构化结果
  `{persisted, archiving}`；Web 存储配额满时显示高优先级警告（要求立即导出项目备份），
  且在完成备份前刷新/关闭页面会触发浏览器确认；只有真正落盘才显示绿色「已导入」。
- **项目导入事务锁（P0-3 轻量）**：导入期间全屏遮罩锁定界面（高于一切弹窗），
  Escape/遮罩点击/二次导入均被拒绝；失败解锁,成功保持锁定直至自动重载。
- **导出 HTML 注入阻断（P1-3）**：分析记录 `statusColor/statusBg` 导入时强制严格
  十六进制校验；项目汇总生成时颜色白名单 + 属性上下文全量转义（含引号）。
- **损坏账本真隔离（P1-4）**：损坏的 AQL 责任账本原文实际复制进隔离区后才提示
  「已隔离」；隔离写入失败时如实说明并保留原文。
- **删除分析如实反馈（P1-6）**：删除未能持久化时保留记录并报错，不再提示「已删除」。
- **统计措辞（P1-7）**：正态性检验通过改述为「未拒绝正态假设」（应用内报告卡、HTML/Office 导出同步更新）。
- **发版前对抗审查追加修复**：本批 diff 经三镜头对抗审查确认 19 项(去重后 9 项),已全部当场修复——
  含全局快捷键受事务遮罩约束(阻断导入窗口期 Ctrl+N/Z 覆盖新项目)、导入成功清除未持久化标记
  (避免 beforeunload 拦截自动重载导致界面锁死)、归档失败回调补 revision 守卫、拖放读取失败不再锁死遮罩、
  四个导出面(文本/HTML/Word·PPT/Excel)的能力判定全部接入唯一判定器、仪表盘 Cpk 卡增加非正态维度、
  工作表粘贴导入接入持久化结果、隔离失败文案不再指向被阻断的导出路径。
  已知未涵盖(low,挂账):单元格编辑/DOE 写回的成功提示仍可能覆盖配额告警(beforeunload 仍设防);
  P/C 图与帕累托数据未持久化时仅弹警告,不触发离开确认。

## Codex 全面审核与工程收口（v1.34.0）

在 v1.33.0 的国标/AQL 闭环上，对统计引擎、全部页面路由、保存/回看、Web/PWA、桌面文件写入和发布流水线做了一轮端到端红队与修复：

- **统计稳定性**：均值、标准差、单/双样本 t 与 ANOVA 改用中心化、补偿求和和缩放计算，修复大基准数据的消减误差；AD 大样本尾部 P 值保持单调；零极差分阶段控制图使用真实零宽控制限，不再注入固定 `1e-12`。
- **AQL 防伪与追溯**：导入批次逐条重查正式表并核对字码、n、Ac/Re、全检与判定，拒绝伪造方案和非法状态转移；加严累计拒收达到 5 批必然进入暂停。AQL 责任账本从界面偏好中独立，项目迁移、清除和重置具有明确边界。
- **P 图与分析口径**：P 图支持逐批样本量 `nᵢ`、加权 `p̄`、逐点控制限和点选明细；真实单列数据不再被双样本 t 静默替换为演示组；不显著结论统一改为“当前样本证据不足”，不再宣称独立、等效或相同。
- **项目与本地数据安全**：`.qproj` 升级为 `formatVersion 2`，新端继续读取 v1，旧端会明确要求升级而不会静默忽略 SQLite vault 或 AQL 账本。配额不足时，工作表、规格/偏好、AQL 和鱼骨图由项目导出读取最新内存快照；损坏缓存进入隔离区，隔离失败则写保护并阻止残缺导出。异步历史恢复、同名超大数据、库删除和项目导入并发均加了竞态守卫。
- **桌面与发布安全**：原生文件导出采用同目录临时文件、`flush/sync_all` 和原子替换，失败保留旧文件；Release 在版本、架构、签名、URL 与 `latest.json` 完整校验通过前保持草稿。PWA 更新改为用户确认后激活，避免旧页面与新 chunk 混用；开发端会主动注销残留生产 Service Worker。
- **GUI 收口**：页面点击、路由、空态、错误态、保存与专项导出按真实数据联动；主操作改用语义控件，模态支持键盘焦点管理，失败通知使用明确错误状态。

## 项目工作台（分析记录对象化）

「保存到项目」把当前分析连同其参数与所属数据集存为一条**记录**:出现在仪表盘
「近期分析记录」与左下「项目管理器」。普通分析点击即**回看**——自动切回当时的数据集、
还原分析参数、跳到对应模块；AQL 属于不能分叉回滚的连续检验流，因此保存的是历史摘要，
点击只返回当前 AQL，不覆盖现行抽样参数、转移状态或责任账本。记录可删除,并随 .qproj 项目文件一起备份/迁移。
这让应用从「算完即走的计算器页面」变为以项目为中心、分析可积累可回看的工作台。

**可命名项目**:项目管理器顶部双击即可重命名当前项目,名称随 .qproj 一起持久化,
并作为项目文件与汇总报告的默认文件名。「文件 → 导出项目汇总…」把项目名、分析总数、
涉及数据集数与全部分析记录(标题/类型/数据集/关键指标/状态)渲染成一份自包含 HTML
报告,双击即可查看或打印——一眼看清整个项目的分析全貌。

## 描述性统计 · 图形化摘要（对标 Minitab Graphical Summary）

「统计 → 描述性统计 / 图形化摘要」(或侧栏「描述性统计」)一屏给出:直方图叠正态曲线、
箱线图(Q1–中位–Q3、须及 1.5·IQR、离群点、均值菱形),以及完整统计表——N、均值、
均值标准误、标准差、方差、变异系数、偏度、峰度、最小/Q1/中位/Q3/最大、极差、IQR、总和,
外加 Anderson-Darling 正态检验与均值/中位数/标准差的 95% 置信区间。计算在
`src/core/descriptive.ts`(纯函数,复用已有 invNorm/quantile/tInv/andersonDarling;标准差
CI 用 Wilson–Hilferty χ² 近似,中位数 CI 用次序统计)。可「保存到项目」为一条分析记录。

## 公式计算列（对标 Minitab 计算器）

「计算 → 公式计算列…」用一条表达式从已有列算出一列新值,如 `(C1 + C2) / 2`、
`(C1 - mean(C1)) / std(C1)`、`sqrt(C1)`。列可用 `C1 C2 …` 或引号列名 `'直径 (mm)'`
引用;支持 `+ − × ÷ ^`、括号、一元负号,逐元素函数(abs/sqrt/ln/log10/exp/round/sq)
与聚合函数(mean/std/var/min/max/sum/median/range/n,整列折算成常量再广播)。
弹窗内实时预览计算结果并即时报错;引擎为自建递归下降解析器(`src/core/formula.ts`,
纯函数、零依赖、不用 eval,可单测),新列追加后可 Ctrl+Z 撤销。

「编辑 → 查找 / 替换…」(Ctrl+F)按数值批量替换测量单元格(3 位小数显示容差,
可限定列,实时命中数,替换可 Ctrl+Z 撤销)。「数据 → 子集 / 条件筛选…」复用同一引擎,按条件表达式筛出子集为新数据集。除算术外,
表达式还支持比较(`> < >= <= == <> !=`,单 `=` 视为等于)与逻辑(`and / or / not`)运算,
如 `C1 > 25`、`C1 >= 24.9 and C1 <= 25.1`、`not (C2 = 0)`;逻辑真值判定排除 NaN。弹窗
实时显示命中行数,条件为真的行保留到新数据集「… (子集)」,原数据集不变。

## 真实数据全面接入（v1.23.0,响应质量工程师验收反馈）

针对「导入自己的数据后出不了图」的反馈,确立**数据契约**并落到多个模块:
真实数据优先、数据形态不满足时显式空态卡(不再静默显示演示)、统一列选择器。

- **帕累托接真实数据**:「导入 → 数据类型选『缺陷类别+频数(帕累托)』」,专用解析器
  健壮支持纵向两列、横向两行(自动转置)、带行标签的横向表、含序号列、空格分隔、
  单列标签自动计数;页面按导入数据出图,空态卡引导,可合并尾部为「其他」。
- **ANOVA 三模式**:分组列(长表)/宽表(各列一组,Minitab unstacked)/空态卡+一键堆叠;
  去除静默回落演示;补区间图与残差四合一;宽表附量纲一致性告警。出厂演示集显示示例徽标。
- **判异准则透明化**:SPC 页常驻「准则 1–4 定义/标准点数/来源(Nelson 1984·Minitab 检验)」,
  开关悬停即见,帮助中心逐条展开。
- **工作表按列自适应显示精度**(3–6 位):混合量纲数据(如 0.0675 与 1350 同表)不再被压成
  0.068 而误解为「数据被改」;悬停显示全精度原值。

## DOE 接真实数据（v1.24.0,响应质量工程师验收反馈·批次B）

针对「DOE 只能看内置 2³ 演示、无法分析自己的实验数据」的反馈,把实验设计模块
从写死的三因子示例升级为**通用两水平全因子引擎**(k=2..4),分「创建设计」与
「分析设计」两条真实数据链路:

- **创建因子设计**:填因子数(2–4)、各因子名与上下限、中心点数、重复数、是否随机化,
  一键生成规范设计表(Yates 标准序 + 中心点取上下限中点 + 确定性 LCG 随机运行序)
  写入工作表,末列「响应(待录入)」留空待试验录入。
- **分析因子设计**:用列选择器勾选因子列与响应列,引擎自动把每个因子识别为 2 水平
  (含中心点=中值),按正交编码算主效应与全部交互(系数 β=Σ(y·x)/Σ(x²)、效应=2β)。
  **有重复或中心点(残差自由度>0)走 t 检验**(给出标准化效应、P 值),
  **饱和设计走 Lenth PSE**(伪标准误 + 显著界限);主效应图/交互作用图(任意因子对)/
  效应帕累托(t 临界或 Lenth 界限)/立方图(3 因子)与**残差四合一诊断**全部按真实数据出图。
- **数据形态不符时给显式原因**:因子多于 4、水平数不为 2(或 3 但中值不在中点)、行数不足
  都会明确提示,并引导去「创建因子设计」生成规范表,不再静默套演示。出厂演示集仍显示 2³ 示例徽标。
- 引擎为纯函数(`src/core/doe.ts`:`detectFactorial` / `analyzeFactorial` /
  `generateFactorialDesign`,零依赖、可单测),`factorial.test.ts` 覆盖与旧 Lenth 一致性、
  含重复的 t 检验路径、含中心点的设计生成。

## 两轮外部审核纠偏(v1.25.0)

在 v1.24.0 后引入 **Codex 与 Cursor(Grok)两个独立模型**做只读代码审核,连同内部对抗审查一并核对。
两轮共确认 11 处真缺陷(0 处被推翻),本版全部落实——其中 3 处是 DOE 引擎里内部审查漏掉的:

- **DOE 未录入拦截**:生成设计后响应列为占位 0、未录入就点分析,过去会给出 P=1.000 等"正式结论"。
  现检测响应列无变异即拦截,提示先按运行序录入,不再把"尚未采集"当成"实测为 0"。
- **零残差非零效应**:完美拟合(MSE=0)时非零效应过去被判"不显著"(方向反了),现修正为无穷显著(p→0)。
- **分数设计别名**:`C=A×B` 等别名结构过去保留交互、丢弃主效应;项枚举改低阶优先,现保住主效应。
- **ANOVA 数值因子模式**:新增"选一数值列作因子(按取值分组)+ 一列作响应"——刘润泽那套
  "压入力 按 过盈量/轴硬度 水平"的单因子 ANOVA 现可正确做出;异量纲宽表给强告警,摘要标"存疑"不再固化无意义的显著 P。
- **SPC 判异一致性**:准则 4 的结果列表改记实际越界点(红点/列表/点击详情三者一致);极差(R/S/MR)图也判异并显示失控点。
- **柏拉图解析**:唯一数值列即使是 1,2,3 也不再当序号删;识别"频率"表头。
- **AQL 方案表**:按当前检验水平重画(水平 I/III 的批量范围与字码不再自相矛盾)、覆盖到批量 15 万、批量输入下限校验。
- **帕累托 80/20**:改判"累计达 80% 所需类别占比",不再"前两类≥60% 即称符合 80/20"。
- **保存快照完整**:ANOVA 模式/列、DOE 因子/响应列纳入分析记录快照,回看与保存时一致。
- **DOE 标准化残差**:四合一改用标准化残差 r/(s·√(1−杠杆)),对标 Minitab。

## Claude 复审问题闭环（v1.33.0）

在 v1.32.0 的人工反馈闭环之上，对 Claude 的 26 条复审意见去重并逐条复现；重复项合并后，修复全部可证实问题，对不符合本版国标原文的结论保留标准口径：

- **表 2-C 逐格纠正**：依据 `GB/T 2828.1-2012 / ISO 2859-1:1999` 原文 PDF 物理第 23 页（印刷第 17 页）重录放宽检验表，纠正 96 格中的 42 格。测试夹具固定 PDF SHA-256 `7a336944…a35e5a37`，2-A/2-B/2-C 共 288 格及 0.40 列 16 格均从版本控制内的独立 TSV 逐格核对，显式保存 Re，不再由生产表复制期望值。
- **转移规则安全闭环**：加严、放宽或第 9.4 条暂停期间锁定 AQL/检验水平，store 层同样拒绝参数绕过；暂停只能在纠正措施确认有效后恢复加严，放宽人工恢复正常也加了核心守卫。正常检验更换体系会生成新 `sequenceId`，保留旧责任账本但不让旧批次冒充当前序列“最近批”。
- **得分与输入真实性**：严一档 AQL 需要不同样本量时，不再用未检验的附加样本假定“仍接收”并恒定 +3，而是保守清零并说明原因；空白不合格品数不再被 `Number('')` 当成 0 提交。
- **全检处置可追溯**：保留国标 Ac/Re 批判定，不把“100% 全检”误写成国标强制 Ac=0；若全检发现不合格品，必须另行记录隔离待处置、返工、替换、报废或批准让步接收。页面和专项报告明确“批接收不等于放行不合格品”。
- **持久化与项目导入**：Zustand v3 每次水合都校验 AQL 参数、状态不变量和责任记录；同版本损坏载荷也不能绕过。`.qproj` 在写入前拒绝非法放宽、矛盾暂停和坏记录，写入中断会回滚。外来文本字段设长度上限，当前体系时间线有界，责任总账不静默截断。
- **历史摘要去重**：新保存的 AQL 历史卡只固化“保存时”的方案摘要和参数，不再把完整责任账本复制最多 40 份；侧栏显示保存日期，回看仍不覆盖活跃检验流。
- **其余审核纠偏**：SPC 的 72 个控制图常数字段改由独立 d2/d3/c4 基础夹具和公开公式全量核对；Nelson 1–8 的精确元数据及 1σ/2σ/3σ 边界全部入测。原文显示本版表 2-C 的 Re 确为 Ac+1，因此未引入旧制表体系的“独立 Re 间隙”；复验批不计入转移规则也按第 9.3 条保留。

## 人工反馈全链路闭环（v1.32.0）

在 v1.30.0 基础上按 4 份人工反馈 PPT、历次审核报告与多 Agent 红队结果完成统一收口：

- **AQL 风险口径**：RQL 改为搜索到 100% 不良率并返回明确三态；旧反例
  N=8/水平 III/AQL 4.0 的真实 RQL 约 53.58%，不再把 40% 搜索边界冒充 β=10% 的 RQL。
  全检模式停用 OC/RQL/α/β，但仍允许记录实际全检结果与追溯字段。更换下一批批量只重查方案，
  不清空正常/加严/放宽状态和转移得分；正常检验期间更换检验水平、AQL 或方法才重建转移序列，同时保留已归档责任记录；加严、放宽与暂停期间由 v1.33.0 起锁定这些参数。
- **AQL 大批量与历史摘要**：批量支持到 JavaScript 安全整数上限并正确落入 P/Q/R 字码档；
  历史项保存当时方案摘要，但回看不回滚仍在演进的连续检验流；v1.33.0 起不再重复复制完整责任账本。
- **SPC 一致性**：分阶段列与 8 条准则纳入快照；页面、图上红点、明细和保存摘要统一按
  实际唯一失控点计数；窗口型准则展开真实红点，I-MR 的 MR 序号恢复到对应原始观测。
- **ANOVA 图形与残差**：新增个体值图；区间图使用 ANOVA 合并误差 MSE/df；真实长表和数值因子
  的残差顺序图恢复工作表行序；常数/全零残差的正态概率图不再产生 NaN。
- **DOE 待录入闭环**：待录入改为独立单元格元数据，重命名无法绕过，实测 0 可明确确认；
  复制、排序、撤销/重做与持久化均保留标记，未录完时禁止会把占位值当观测的派生操作；
  SPC、能力、Gage R&R 与 DOE 也统一拦截当前分析角色内的待录入值；同时拒绝“各因子中值分散在不同行”的伪中心点设计。
- **帕累托导入与回看**：多数值列可手动指定类别列/频数列，数值类别代码可保留；
  “其他”累计阈值可调，数据和显示设置随分析记录一起保存、恢复。
- **演示语义清理**：真实数据的仪表盘与文本报告不再混入固定的“三台设备直径”ANOVA 告警；
  关于页与文本报告显示实际应用版本。
- **数据角色与历史一致**：SPC 支持行/列/长表/单值四种形态，数值「子组」ID 不再混入测量；
  名称不同的业务数值列不会再被自动当成同一特性的组内重复值，DOE 元数据及拟合值/残差等存储输出也不会反向污染 SPC/能力；
  SPC、P/C、能力、Gage R&R、帕累托和鱼骨图保存当时数据/角色/规格快照，回看不受后续导入或规格记忆覆盖，
  缺少必要快照的旧记录明确拦截，不拿当前数据冒充历史结果。
- **ANOVA / DOE 完整输出**：ANOVA 统一输出 DF/SS/MS/F/P、合并误差 CI、个体值图和工作表顺序残差；
  DOE 支持 2–4 因子、标准/运行序、区组/点类型、模型项/曲率/别名；区组设计的主效应、交互图与优化均使用区组校正模型，
  曲率显著、无显著效应或最优组合并列时不再给出虚假的唯一推荐，并可回写拟合值、残差等结果。
- **假设检验边界**：单/双样本 t 与回归明确处理零方差、完美线性及待录入值，避免产生 NaN 或把占位值当观测。
- **当前分析导出**：AQL、ANOVA、单双样本 t、回归、DOE、全部 SPC 图型、能力、Gage R&R、帕累托和鱼骨图
  统一导出当前结果；未运行与内置示例明确标记，Excel 不夹带无关数据、伪子组均值/极差或通用能力摘要。

当前 AQL 产品路径已固定使用 GB/T 2828.1-2012 / ISO 2859-1:1999 表 1 与正式表 2-A/2-B/2-C；
16 个初始字码 × 6 个常用 AQL × 3 张表均有逐格回归测试。正常↔加严↔放宽使用第 9.3/9.4 条的正式转移得分、管理条件与暂停/恢复规则，不再使用“Ac−1/字码降两档”近似。

## AQL n≥N 转 100% 全检 + 文档/测试纠偏(v1.30.0)

对 v1.29.0 做了一轮 5 镜头对抗审查(并逐条独立验证),确认并修复 8 处:

- **n ≥ 批量 N → 100% 全检(真缺陷)**:小批量下箭头改档得到的样本量可能超过批量(如批量 8 查表得 n=20)。
  按 GB/T 2828.1,此时应对整批逐件检验。`aqlPlan` 现将 n 夹到批量并置 `fullInspect` 标志,AQL 页显示醒目「100% 全检」提示,方案表对应档标「全检」。
- **帮助文档纠偏**:F1 帮助原称「Ac 满足 Pa(AQL)≥95%」,与默认真表 Ac=0 方案(Pa≈88%)矛盾——改为如实区分两法。
- **加严/放宽简化模型文案**:Ac 已为 0 时加严不再减、字码到最小档 A/B 时放宽无法再降,脚注均已注明。
- **测试补强**:补 AQL 1.5/6.5 列锚点、全检行为、三态 Ac=0 边界;把一处名不副实的伪测试改为真调用 `aqlPlan` 的默认字码法守护。301 测试全绿。

## AQL 接收数·真国标主表 2-A 逐格查表(v1.29.0)

AQL 页开关**「接收数 Ac:国标主表 / 二项近似」**:

- **国标主表(默认)**:按 **GB/T 2828.1-2012 表 2-A(正常检验一次抽样主表)逐格查表**,
  含 **↑↓ 箭头改档**——当某格为箭头时,会**整格一起改变字码 / 样本量 n / Ac**(取箭头指向的行)。
  例:N=120/水平 II/AQL 1.0,初始字码 F,该格为 ↑,改档为 **E / n=13 / Ac=0 / Re=1**。覆盖常用 AQL 0.65–6.5。
- **二项近似**:取最小满足 95% 接收概率的接收数(原算法,保留为可选项)。

表 2-A 的 96 格由 **Codex 依国标原页逐格转录、Grok 独立复核**,并在本仓再做三重校验:
① 与源 CSV 逐格对拍(96/96 完全一致);② 每格最终字码的 n 与 `N_BY_CODE` 一致;③ ~15 个锚点手工核对。
数据落在 `src/core/aql.ts` 的 `MASTER_2A_RAW`,查表函数 `masterPlan2A`。

⚠ **历史勘误(v1.27.0→v1.28.0→v1.29.0)**:v1.27.0 曾把"优先数吸附"近似误称"国标数列",
被外部审核(Codex/Grok)逐格对拍判为**确有多格与真表不符**(F/1.0、N/4.0、G/2.5 等,且缺箭头改档);
v1.28.0 先纠正表述、改名"优先数近似";**v1.29.0 用上方真表 2-A 逐格查表彻底替换,不再是近似。**
字码与样本量 n 的国标查表(v1.26.0)是真查表、正确。

## 判异准则 8 条 + AQL 字码可选国标查表(v1.26.0)

按两位质量工程师的偏好,两项从"够用近似"升级为"标准完整,且用户可选":

- **判异准则 1–8(完整 Nelson / Minitab 特殊原因检验)**:从原来的 4 条扩为标准 8 条,
  控制图工具条逐条可勾选、常驻说明区与帮助中心同步列出:1 越 3σ / 2 连续 9 点同侧 /
  3 连续 6 点升降 / 4 连续 14 点交替 / 5 相邻 3 点 2 点越 2σ / 6 相邻 5 点 4 点越 1σ /
  7 连续 15 点全在 1σ 内 / 8 连续 8 点全在 1σ 外。默认开启经典四则(1/2/3/5),
  4/6/7/8 较敏感按需开启;旧偏好经迁移把原"准则4(2 of 3)"平滑映射到标准检验 5。
- **AQL 字码判定「国标查表 / 位移近似」二选一**:AQL 页新增开关——
  「国标查表」按真实 GB/T 2828.1 样本量字码表(批量×检验水平直接查、覆盖到批量 50 万+、
  含字码 P/Q/R),「位移近似」保留以水平 II 为基准 ∓2 位的旧算法。二者对水平 I/III 常给不同字码
  (如 N=120 水平 III:国标 G、近似 H),用户按需选择;方案表随所选方法与水平实时重画。
- 引擎纯函数化:`evalRules` 八则、`codeLetterGB` / `codeLetterShift` / `CODE_LETTER_TABLE`
  均可单测(`batchD.test.ts` 覆盖各则边界与国标/位移差异)。

## 助手结论卡（对标 Minitab Assistant Report Card）

每个分析页顶部有一张**红绿灯结论卡**:一句人话结论 + 逐项体检,不懂统计也能直接读懂
"过了没有、该干什么"。覆盖:SPC(稳定性/数据量/子组结构,失控按准则汇总)、过程能力
(Cpk 分级/AD 正态性/样本量/过程稳定性——不稳时诚实提示能力数值无预测意义)、
Gage R&R(AIAG 10%/30% 判据)、单因子 ANOVA、单/双样本 t、回归(R² 分级 + 显著性)。
builder 为纯函数(`src/ui/reportData.ts`,可完整单测),视图 `ReportCard.tsx`。

## 分析助手（对标 Minitab Assistant）

不确定该用哪个分析?左侧「助手 · 选分析」用一棵决策树带你选:回答「你想解决什么问题」
「数据是哪种类型」「比较几组」等几个问题,助手就推荐合适的模块,并用三句话说明
**这个分析做什么 / 需要什么数据 / 为什么推荐它**,点「开始分析」即自动套用正确的
控制图类型或检验方法(如 X̄-R / I-MR / P 图、单样本 / 双样本 t / ANOVA)并跳转进入。
决策树覆盖全部 8 个分析模块(`src/ui/assistant/decisionTree.ts`,纯数据、可单测)。

## 本机数据集库（桌面端 SQLite 后端）

桌面版内置 SQLite(`src-tauri/src/db.rs`,rusqlite bundled,库文件在应用数据目录
`datasets.db`):每次导入或编辑的数据集都自动镜像归档(400ms 防抖),容量不受浏览器
localStorage(约 5MB)限制——十万行级数据集也能保存找回。「数据 → 数据集库(本机)…」
查看全部归档(名称/时间/大小),可加载回工作区或删除;从「最近列表」移除的数据集
仍可在库中找回。Web/便携版不受影响(该菜单显示桌面版提示),原有 localStorage 与
.qproj 通道保持不变——这是**增量**后端,不替换任何现有持久化路径。

**大数据集无忧(阶段 2)**:活动数据集写入 localStorage 超出配额时,桌面端自动改存
SQLite 库并留恢复标记——重启后自动恢复到该数据集;最近列表同步瘦身为轻量条目
(点击时从库回落加载),撤销/重做同样兜底。也就是说,桌面版导入十万行级数据集:
能用、重启不丢、最近列表可点回。**.qproj 全量迁移**:桌面端导出项目文件时把库中
数据集(超大集与被挤出最近列表的历史集)一并嵌入,换机导入自动恢复进对方的库
(Web 端导入时跳过库数据并提示)。当前导出使用 **formatVersion 2**：v1 项目仍可由新版本读取；旧版本遇到 v2 会明确提示升级，避免静默忽略 vault、活动引用或独立 AQL 账本后产生“导入成功但数据不全”。

**大数据渲染(阶段 3)**:图表渲染降采样——控制图超 600 点时 min-max 分桶保形抽样
(失控点/选中点/段边界必留,图上标注「渲染 M/N 点」),概率图/散点图同样封顶;
**统计与判异始终基于全量数据**,降采样只影响画多少个 SVG 节点。工作表超 400 行
启用行虚拟化(只渲染可视窗口,滚动条由等高占位撑开),两万行滚动依然流畅。
「生成随机数据」子组数上限放宽到 100000,便于体验大数据链路。
(引擎级修复:全代码库消除 `Math.min(...大数组)` 展开调用——V8 约 6.5 万参数即栈溢出,
涉及 CSV 导入、公式聚合、控制图/直方图/散点图/箱线图轴范围与列统计。)

## 本地单机使用（零网络依赖）

- **项目文件 (.qproj)**：「文件 → 保存项目文件… / Ctrl+S」把全部本地数据（数据集、
  最近列表、已保存分析、规格记忆、鱼骨图、偏好）导出为单个文件;在任何机器上「打开…」
  选择该文件即完整恢复——备份与跨机迁移的唯一通道,无需任何服务。
- **单文件便携版**：`npm run build:portable` 产出一个自包含 HTML
  （Release 资产 `quality-platform-portable-x.y.z.html`）,拷贝到任意机器用浏览器
  打开（file://）即可使用,零安装零网络;数据同样可经 .qproj 进出。
- 应用运行时无任何外部网络请求（字体本地打包,PWA 离线,更新检查失败静默降级）。

## 后续

- MES 真实协议接入（串口 modbus / OPC-UA,替换模拟流）
- SQLite 数据集库的长期保留策略、压缩与维护工具
- OS 代码签名（需购买证书;更新器签名已就绪,私钥在 GH Secret
  `TAURI_SIGNING_PRIVATE_KEY`,本地备份 `~/.tauri/quality-platform.key` —— **务必备份,丢失则无法再发更新**）
- GitHub Pages 在线版：`pages.yml` 已就绪,仓库转 Public（或升级付费计划）后手动触发
